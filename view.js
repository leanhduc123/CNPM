const view = {}

view.showComponent = function(name) {
    switch(name) {
        case "register": {

            break
        }
        case "logIn": {
            
            break
        }
    }
}